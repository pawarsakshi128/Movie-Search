// lib/theme/colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const darkGray = Color(0xFF212121);
  static const accentGreen = Color(0xFF5EC570);
  static const primaryBlue = Color(0xFF1C7EEB);
  static const secondaryDark = Color(0xFF262E2E);
  static const white = Color(0xFFFFFFFF);

  // Gradient
  static const gradientColors = [
    primaryBlue,
    accentGreen,
  ];
}